{
  "name": "saes-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "npm run copy",
    "copy": "mkdir -p dist && cp -r index.html favicon.svg src dist/ 2>/dev/null || (if exist dist (rmdir /s /q dist) && mkdir dist && robocopy . dist index.html favicon.svg /COPY:DAT && robocopy src dist/src /S /COPY:DAT)"
  }
}
